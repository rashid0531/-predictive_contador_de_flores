# a=[1,2,3,1,3]
# b=[4,5,6]
# z=10
# c = list(map(lambda x,y: x+y,a,b))
#
# import tensorflow as tf
#
# one_hot = tf.one_hot(a, 5)
# sess = tf.InteractiveSession()
# one_hot= sess.run(one_hot)
#
# print(one_hot)
#

